// component(ls-resize-observer)
import component from './resize-observer';
import { defineInstallableComponent } from '<assets>/scripts/util/common/plugin';

export default defineInstallableComponent(component);